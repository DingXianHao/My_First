def print_mul_sheet(row):
    """
    打印乘法口诀表
    :param row: 要答应的行数
    :return: None
    """
    for i in range(1, row+1):
        for j in range(1, i+1):
            print("{0}*{1}={2:<}".format(i, j, i*j), end='\t')
        print('')
    return None

print_mul_sheet(9)
